/**
 * Integration-related constants and enums
 */

export enum IntegrationType {
  WHATSAPP = "whatsapp",
  TELEGRAM = "telegram",
}

export enum ConnectionStatus {
  CONNECTED = "connected",
  DISCONNECTED = "disconnected",
  ERROR = "error",
}

export enum CredentialAction {
  ADDED = "added",
  UPDATED = "updated",
  TESTED = "tested",
  REVOKED = "revoked",
}

